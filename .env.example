# Puerto en el que corre el servidor (Render/Railway lo setean solos, para local usa 8787)
PORT=8787

# Cadena de conexión a tu base de datos Postgres (te la da Neon, Render, Railway, etc.)
# Ejemplo Neon: postgres://usuario:password@ep-xxxx.neon.tech/negocio?sslmode=require
DATABASE_URL=
